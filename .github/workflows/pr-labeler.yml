name: PR Labeler

on:
  pull_request:
    types: [opened]

jobs:
  pr-labeler:
    name: PR Labeler
    runs-on: ubuntu-20.04
    steps:
      - uses: TimonVS/pr-labeler-action@v3.1.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
